# 開発: docker compose up

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/rails
      - ~/.aws:/root/.aws:ro # AWS CLIの設定ファイルをマウント
    environment:
      - RAILS_ENV=development
    command: ["./bin/rails", "server", "-b", "0.0.0.0"]
