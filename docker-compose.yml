# 開発: docker compose up
# 本番ビルド: docker build -t app .

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/rails
    environment:
      - RAILS_ENV=development
    command: ["./bin/rails", "server", "-b", "0.0.0.0"]
